Index: /usr/share/genkernel/gen_initramfs.sh
===================================================================
--- a/gen_initramfs.sh
+++ b/gen_initramfs.sh
@@ -303,7 +303,9 @@ append_base_layout() {
 	print_info 2 "$(get_indent 2)>> Adding /etc/ld.so.conf ..."
 	cat >"${TDIR}"/etc/ld.so.conf <<-EOF
 	# ld.so.conf generated by genkernel
-	include ld.so.conf.d/*.conf
+  # this needs to be absolute path, or else: * Failed to pre-generate '/var/tmp/genkernel/gk_cLg8jJSd/initramfs-final/etc/ld.so.cache'! Probably due to sandbox/permission problem; Ignoring ...
+  # that's because: "ldconfig: need absolute file name for configuration file when using -r"
+	include /etc/ld.so.conf.d/*.conf
 	/lib
 	/usr/lib
 	EOF
@@ -1840,8 +1842,25 @@ create_initramfs() {
 			# to actually match initramfs' content.
 			print_info 1 "$(get_indent 1)>> Pre-generating initramfs' /etc/ld.so.cache ..."
 			# Need to disable sandbox which doesn't understand chroot(), bug #431038
-			SANDBOX_ON=0 ldconfig -f /etc/ld.so.conf -r "${TDIR}" 2>/dev/null \
-				|| print_warning 1 "Failed to pre-generate '${TDIR}/etc/ld.so.cache'! Probably due to sandbox/permission problem; Ignoring ..."
+      #this says: "ldconfig: need absolute file name for configuration file when using -r" but that -f arg is already absolute so wtf?!
+      ls -la -- "${TDIR}/etc/ld.so.cache"  #yep this already exists, 103406 bytes
+      #^ here, it's owned by _kernelbuilder:_kernelbuilder
+      local ec
+			SANDBOX_ON=0 ldconfig -f /etc/ld.so.conf -r "${TDIR}" ; ec="$?"
+			#SANDBOX_ON=0 ldconfig -r "${TDIR}" #no change, because it's /etc/env.d/00glibc having a relative include instead of an absolute one!
+      #SANDBOX_ON=0 touch unsandboxed
+      #SANDBOX_ON=1 touch sandboxed
+      #touch unspecified
+      #^ these all remain owned by _kernelbuilder:_kernelbuilder
+      #so why would, at the end of genkernel, yield these two non-_kernelbuilder owned:
+      #$ sudo find /var/tmp/genkernel/ \! -user _kernelbuilder -exec ls -la "{}" \;
+      #-rw-r--r-- 1 root root 352 Apr  1 04:09 /var/tmp/genkernel/gk_h4CxMTw4/initramfs-final/etc/ld.so.cache
+      #lrwxrwxrwx 1 root root 13 Apr  1 04:09 /var/tmp/genkernel/gk_h4CxMTw4/initramfs-final/lib/libnss_dns.so.2 -> libnss_dns.so
+      ls -la -- "${TDIR}/etc/ld.so.cache"  #new one is 352 bytes
+      #^ here, it's owned by _kernelbuilder:_kernelbuilder
+      if test "$ec" -ne "0"; then
+        print_warning 1 "Failed to pre-generate '$(ls -la -- "${TDIR}/etc/ld.so.cache")' exitcode='$ec Probably due to sandbox/permission problem; Ignoring ..."
+      fi
 		fi
 
 		find . -print0 | sort -z | "${CPIO_COMMAND}" ${CPIO_ARGS} --reproducible -F "${CPIO_ARCHIVE}" 2>/dev/null \
